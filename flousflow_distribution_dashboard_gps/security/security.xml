<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- GPS dashboard extension: the vehicle/GPS manager groups see the
         dashboard too. -->
    <record id="flousflow_distribution_route_base.group_distribution_route_manager" model="res.groups">
        <field name="implied_ids" eval="[(4, ref('flousflow_distribution_dashboard.group_distribution_dashboard_user'))]"/>
    </record>

</odoo>
